name: Social Readme

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  update-social:
    runs-on: ubuntu-latest
    steps:
      - uses: AllenCheng5186/AllenCheng5186
        with:
          blog_rss_link: https://studywithallen.com/feed/
          blog_limit: 5 
